"use client";

interface Props {
  instanceId: string;
}

export default function ChartModule({ instanceId }: Props) {
  return (
    <div className="flex items-center justify-center h-full w-full text-white/30 text-sm">
      Grafik modulu - yapim asamasinda
    </div>
  );
}
